<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Activity 正常主题 -->
    <style name="Theme.Sync" parent="android:Theme.Material.Light.NoActionBar" />

    <!-- Splash Screen 主题：使用 app 图标，背景色与 Theme.Sync 一致 -->
    <style name="Theme.Sync.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>
        <item name="postSplashScreenTheme">@style/Theme.Sync</item>
    </style>

</resources>